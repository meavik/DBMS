DECLARE
NUM INTEGER:= 121;
REV INTEGER := 0;
TEMP INTEGER;
BEGIN
TEMP := NUM;
WHILE (TEMP>0) LOOP
REV := (REV*10) + MOD(TEMP,10);
TEMP := TEMP/10;
END LOOP;
IF (NUM=REV) THEN
DBMS_OUTPUT.PUT_LINE('PALINDROME');
ELSE
DBMS_OUTPUT.PUT_LINE('NOT PALINDROME');
END IF;
END;
